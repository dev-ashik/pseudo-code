Time complexcity = n^2
insertion Sort ascending(1, 4, 6)

FOR i 1 TO array.length
    key = array[i]
    j = i-1

    WHILE(j>=0 AND key < array[j])
        array[j+1] <-- array[j]
        j -= 1
    array[j+1] <-- key




insertion Sort descending(6, 3, 1)

FOR j <-- 2 TO length[A]
    do key <-- A[j]
    i <-- j-1
    WHILE i>0 and A[i] > key
        do A[i+1] <-- A[i]
        i <-- i-1
    A[i+1] <-- key